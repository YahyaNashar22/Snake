namespace Snake
{
    // Represents the values in the grid to determine different aspects of the game objects
   public enum GridValue
    {
        Empty,
        Snake,
        Food,
        Outside
    }
}
